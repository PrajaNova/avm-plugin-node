name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write      # build provenance attestations (PrajaNova/avm#19)
  attestations: write

jobs:
  release:
    uses: PrajaNova/avm/.github/workflows/plugin-release.yml@chore/simplify-cleanup
    secrets: inherit
